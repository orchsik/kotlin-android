<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.orchsik.androidmessage">

    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AndroidMessage">
        <activity android:name=".NotificationActivity4"></activity>
        <activity android:name=".NotificationActivity3" />
        <activity android:name=".NotificationActivity2" />
        <activity android:name=".NotificationActivity1" />
        <activity android:name=".PendingIntent" />
        <activity android:name=".Notification" />
        <activity android:name=".ChoiceListDialog" />
        <activity android:name=".ListDialog" />
        <activity android:name=".Dialog" />
        <activity android:name=".SnackBar" />
        <activity android:name=".Toast" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>